(rule
 (target large_fixture.ml)
 ; biggest file in the repo
 (deps ../../lib/ansi_renderer/snippet.ml)
 (action
  (with-stdout-to
   %{target}
   (progn
    (echo "let large_file = {|")
    (cat %{deps})
    (echo "|}")))))

(library
 (name test_ansi_renderer)
 (libraries core fmt grace grace.ansi_renderer dedent)
 (inline_tests)
 (preprocess
  (pps ppx_jane)))
